# Failure-to-Regression Template

Use this when campaign friction exposes a generic engine defect.

```text
Lesson ID: LESSON-NNNN
Origin run/evidence:
Affected capability/version:

Observed generic failure:

Minimal abstract reproduction:
- state/input A:
- state/input B:
- operation:
- expected:
- actual:

Target semantics removed:
yes/no

Regression fixture ID:

Required invariant:

Forge change requested:

Before metrics:

After metrics:

All prior Crucible suites pass:
yes/no

Published capability version:
```

The objective is to preserve the algorithmic lesson without passing target-specific offensive semantics into Forge.
